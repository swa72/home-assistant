#-------------------------------------------------------------
#   Send Message to swa - Central Notify Command
#-------------------------------------------------------------
notify_swaiphone:
  alias: 'Send Message to swa'
  sequence:
    - service: notify.mobile_app_swaiphone
      data_template:
        title: "{{ title }} (mobile_app)"
        message: "{{ message }}"
